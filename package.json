{
  "name": "lynx-capacitor",
  "version": "0.1.0",
  "private": true,
  "description": "Run Capacitor plugins on Lynx. An adapter reimplementing the Capacitor bridge over the Lynx NativeModule API, plus a demo plugin gallery for iOS.",
  "license": "MIT",
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "build:core": "pnpm --filter @lynx-capacitor/core build",
    "dev:demo": "pnpm --filter @lynx-capacitor/demo dev",
    "build:demo": "pnpm --filter @lynx-capacitor/demo build",
    "ios:generate": "bash scripts/ios-generate.sh",
    "ios:build": "bash scripts/ios-build.sh",
    "ios:run": "bash scripts/ios-run.sh",
    "verify": "bash scripts/verify.sh"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2"
  }
}
